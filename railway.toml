[build]
builder = "DOCKERFILE"

[deploy]
# No healthcheckPath here — this config is shared between API and worker services.
# The worker has no HTTP server so an HTTP healthcheck would always fail.
# The API still healthchecks itself via the GitHub App webhook + uvicorn startup logs.
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
